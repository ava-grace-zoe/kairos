{
  "name": "kairos",
  "private": true,
  "workspaces": [
    "mcps/*",
    "shared"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "link:dev": " npx skills add ./"
  }
}
